<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neusoft.coursemgr.mapper.ReviewMapper">

    <resultMap id="ReviewResultMap" type="com.neusoft.coursemgr.domain.Review">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="orderId" column="order_id" />
        <result property="productId" column="product_id" />
        <result property="rating" column="rating" />
        <result property="content" column="content" />
        <result property="createTime" column="create_time" />
    </resultMap>

    <select id="selectByUserOrderProduct" resultMap="ReviewResultMap">
        SELECT id, user_id, order_id, product_id, rating, content, create_time
        FROM review_table
        WHERE user_id = #{userId}
          AND order_id = #{orderId}
          AND product_id = #{productId}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.neusoft.coursemgr.domain.Review" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review_table (user_id, order_id, product_id, rating, content)
        VALUES (#{userId}, #{orderId}, #{productId}, #{rating}, #{content})
    </insert>

    <delete id="deleteByProductId">
        DELETE FROM review_table WHERE product_id = #{productId}
    </delete>

    <select id="selectByProductId" resultMap="ReviewResultMap">
        SELECT id, user_id, order_id, product_id, rating, content, create_time
        FROM review_table
        WHERE product_id = #{productId}
        ORDER BY id DESC
    </select>

</mapper>
