<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neusoft.coursemgr.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.neusoft.coursemgr.domain.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="passwordHash" column="password_hash" />
        <result property="role" column="role" />
        <result property="phone" column="phone" />
        <result property="email" column="email" />
        <result property="realName" column="real_name" />
        <result property="avatarUrl" column="avatar_url" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password_hash, role, phone, email, real_name, avatar_url, status, create_time, update_time
    </sql>

    <select id="selectById" parameterType="long" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user_table WHERE id = #{id}
    </select>

    <select id="selectByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user_table WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.neusoft.coursemgr.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_table (username, password_hash, role, status)
        VALUES (#{username}, #{passwordHash}, #{role}, #{status})
    </insert>

    <update id="updateProfile">
        UPDATE user_table
        SET phone = #{phone},
            email = #{email},
            real_name = #{realName},
            avatar_url = #{avatarUrl}
        WHERE id = #{id}
    </update>

    <select id="selectUsers" parameterType="string" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_table
        <where>
            <if test="keyword != null and keyword != ''">
                (username LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%')
                OR real_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>
